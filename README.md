# Buge-Code-Integrator
Code Integrator - Android app (Kotlin, Material 3) that merges source files into one text file. Features: folder tree, file extraction, custom filters, dark theme, rounded UI, auto-exclude build folders. Output: SourceCodeIntegration.txt in selected folder.
